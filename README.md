Simple agentic code editor in Python, similar to Claude Code or Cursor's Agent Mode.  It uses the Google Gemini API for an LLM-powered CLI code agent.

**WARNING! The tool doesn't have all the security and safety features that a production AI agent should have. This is for learning purposes only.**